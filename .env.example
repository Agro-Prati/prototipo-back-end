# ===================================
# Configurações do Backend - AgroMais
# Copie este arquivo para .env e ajuste os valores
# ⚠️ NUNCA commite o arquivo .env com valores reais!
# ===================================

# Database PostgreSQL
DB_HOST=localhost
DB_PORT=5433
DB_NAME=agro_prototipo_db
DB_USER=postgres
DB_PASSWORD=postgres

# Docker PostgreSQL (usado pelo docker-compose.yaml)
# ⚠️ ALTERE estas senhas em produção!
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=agro_prototipo_db
POSTGRES_PORT=5433

# Docker PgAdmin
# ⚠️ ALTERE estas credenciais em produção!
PGADMIN_DEFAULT_EMAIL=admin@admin.com
PGADMIN_DEFAULT_PASSWORD=admin
PGADMIN_PORT=8081

# JWT Configuration
# ⚠️ OBRIGATÓRIO: Gere um secret forte e único para produção!
# Exemplo de geração: openssl rand -base64 64
JWT_SECRET=agromais-secret-key-change-in-production-2025-make-it-long-enough
JWT_EXPIRATION=86400000

# Google OAuth2
# ⚠️ OBRIGATÓRIO: Configure suas credenciais do Google Cloud Console
# https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=seu-client-id-aqui
GOOGLE_CLIENT_SECRET=seu-client-secret-aqui

# Server Configuration
SERVER_PORT=8080

# CORS Configuration (URLs separadas por vírgula)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:4200

# Logging
LOGGING_LEVEL_ROOT=INFO
LOGGING_LEVEL_COM_AGROMAISPRATI=DEBUG

# Spring Profile (dev, prod, test)
# ⚠️ Use 'dev' apenas em desenvolvimento (cria usuários de teste automaticamente)
SPRING_PROFILES_ACTIVE=dev
